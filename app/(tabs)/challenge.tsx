import React from 'react';
import { View, Text } from 'react-native';

const challenge = () => {
    return (
        <View>
            <Text>challenge</Text>
        </View>
    );
};

export default challenge;
